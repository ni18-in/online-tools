# Astro build output (deployed via GitHub Actions, never committed)
dist/
# Astro generated types
.astro/
# dependencies
node_modules/
# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
# environment
.env
.env.*
!.env.example
# test artifacts
test-results/
playwright-report/
node_modules/
.lighthouseci/
# OS
.DS_Store
Thumbs.db
node_modules
# stray dev-server output (was committed by accident)
*.log
http_server.log
preview_server.log
# local Claude Code permission state — machine-specific, not shared config
.claude/settings.local.json
