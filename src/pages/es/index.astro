---
import PageLayout from '../../layouts/PageLayout.astro';
import ToolCard from '../../components/ToolCard.astro';
import type { Props as SeoProps } from '../../components/Seo.astro';
import { SITE_URL, SITE_NAME, DEFAULT_OG_IMAGE } from '../../data/site';
import { homepageGrid, homepageItemListOrder, getTool, homepageItemListItem, TOOL_TRANSLATIONS } from '../../data/tools';

const collectionItems = homepageItemListOrder.map((slug, i) => {
  const t = getTool(slug)!;
  const item = homepageItemListItem(t);
  // Localize ItemList schema for SEO
  if (t.i18n?.es) {
    item.url = `${SITE_URL}/es/tools/${t.i18n.es}/`;
    if (TOOL_TRANSLATIONS[t.slug]?.es) {
      item.name = TOOL_TRANSLATIONS[t.slug].es.name;
      item.description = TOOL_TRANSLATIONS[t.slug].es.description;
    }
  }
  return {
    '@type': 'ListItem',
    position: i + 1,
    item,
  };
});

const seo: SeoProps = {
  title: 'NI18 | Herramientas Web Online Gratuitas y Conversores',
  description: 'Tu destino para herramientas web gratuitas. Accede a calculadoras, utilidades de texto, conversores y más utilidades directamente en tu navegador de forma rápida y privada.',
  canonical: `${SITE_URL}/es/`,
  keywords: 'herramientas online, utilidades web gratis, conversores, calculadoras, desarrolladores',
  hreflang: [
    { hreflang: 'en', href: `${SITE_URL}/` },
    { hreflang: 'es', href: `${SITE_URL}/es/` },
    { hreflang: 'fr', href: `${SITE_URL}/fr/` },
    { hreflang: 'x-default', href: `${SITE_URL}/` },
  ],
  og: {
    type: 'website',
    url: `${SITE_URL}/es/`,
    title: 'NI18 | Herramientas Web Online Gratuitas',
    description: 'Colección de herramientas web gratuitas en tu navegador.',
    image: DEFAULT_OG_IMAGE,
    siteName: SITE_NAME,
    locale: 'es_ES',
  },
  twitter: {
    card: 'summary_large_image',
    url: `${SITE_URL}/es/`,
    title: 'NI18 | Herramientas Web Online Gratuitas',
    description: 'Colección de herramientas web gratuitas en tu navegador.',
    image: DEFAULT_OG_IMAGE,
  },
  jsonLd: [
    {
      '@context': 'https://schema.org',
      '@type': 'WebSite',
      name: 'NI18 Herramientas Online',
      url: `${SITE_URL}/es/`,
      inLanguage: 'es',
    },
    {
      '@context': 'https://schema.org',
      '@type': 'CollectionPage',
      name: 'Herramientas Online - Conversores y Calculadoras Gratuitas',
      description: 'Colección de herramientas en línea gratuitas que se ejecutan directamente en tu navegador de forma privada.',
      url: `${SITE_URL}/es/`,
      mainEntity: { '@type': 'ItemList', itemListElement: collectionItems },
    },
  ],
};

// Resolve cards, localizing name and description
const cards = homepageGrid.map((entry) => {
  if (typeof entry === 'string') {
    const t = getTool(entry)!;
    const esSlug = t.i18n?.es || t.slug;
    const trans = TOOL_TRANSLATIONS[t.slug]?.es;
    return {
      href: `/es/tools/${esSlug}/`,
      name: trans?.name || t.name,
      description: trans?.description || t.description,
      external: false,
    };
  }
  return {
    href: entry.external,
    name: entry.name,
    description: entry.description,
    external: true,
  };
});
---
<PageLayout seo={seo} lang="es" current="home" adsEnabled={true}>
  <section class="hero">
    <h1>Herramientas Web Gratuitas</h1>
    <p>Calculadoras, conversores y utilidades para desarrolladores — rápidas, privadas y directamente en tu navegador.</p>
  </section>

  <section aria-labelledby="search-heading" class="search">
    <h2 id="search-heading" class="sr-only">Buscar Herramientas</h2>
    <form role="search" id="tool-search-form" onsubmit="return false;">
      <label for="tool-search-input" class="sr-only">Buscar herramientas</label>
      <input type="search" id="tool-search-input" name="q" placeholder="Buscar herramientas por nombre o descripción..." autocomplete="off" />
    </form>
  </section>

  <section aria-labelledby="tool-list-heading">
    <h2 id="tool-list-heading" class="section-title">Herramientas Disponibles</h2>
    <p id="no-results-message" class="no-results" style="display:none" aria-live="polite">
      No se encontraron herramientas que coincidan con tu búsqueda.
    </p>
    <div class="tool-grid">
      {cards.map((c, i) => (
        <ToolCard
          href={c.href}
          name={c.name}
          description={c.description}
          headingId={`tool-heading-${i + 1}`}
          external={c.external}
          cta="Iniciar"
        />
      ))}
    </div>
  </section>

  <script is:inline>
    (function () {
      var input = document.getElementById('tool-search-input');
      var cards = Array.prototype.slice.call(document.querySelectorAll('.tool-card'));
      var noResults = document.getElementById('no-results-message');
      if (!input || !cards.length) return;
      input.addEventListener('input', function () {
        var q = input.value.toLowerCase().trim();
        var found = 0;
        cards.forEach(function (card) {
          var a = card.querySelector('h3 a');
          var p = card.querySelector('p');
          var title = ((a && a.textContent) || '').toLowerCase();
          var desc = ((p && p.textContent) || '').toLowerCase();
          var visible = !q || title.indexOf(q) !== -1 || desc.indexOf(q) !== -1;
          card.style.display = visible ? '' : 'none';
          if (visible) found++;
        });
        if (noResults) noResults.style.display = found === 0 && q ? 'block' : 'none';
      });
    })();
  </script>
</PageLayout>

<style>
  .hero { text-align: center; padding: 24px 0 8px; }
  .hero h1 { font-size: 2.25rem; margin: 0 0 8px; color: var(--md-sys-color-on-surface); }
  .hero p { margin: 0 auto; max-width: 38rem; color: var(--md-sys-color-on-surface-variant); }
  .search { max-width: 42rem; margin: 24px auto; }
  #tool-search-input {
    width: 100%;
    padding: 14px 20px;
    font-size: 1rem;
    border: 1px solid var(--md-sys-color-outline);
    border-radius: 999px;
    background: var(--md-sys-color-surface-container-lowest);
    color: var(--md-sys-color-on-surface);
  }
  #tool-search-input:focus { outline: 2px solid var(--md-sys-color-primary); border-color: transparent; }
  .section-title { text-align: center; font-size: 1.75rem; margin: 16px 0 24px; color: var(--md-sys-color-on-surface); }
  .no-results { text-align: center; color: var(--md-sys-color-on-surface-variant); padding: 40px 0; }
  .tool-grid { display: grid; grid-template-columns: 1fr; gap: 24px; }
  @media (min-width: 640px) { .tool-grid { grid-template-columns: repeat(2, 1fr); } }
  @media (min-width: 1024px) { .tool-grid { grid-template-columns: repeat(3, 1fr); } }
</style>
